
node_modules
.next

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Vendored npm package directories (installed by npm install)
@img/
@next/
__tests__/

# Local environment files and demo credential files (never commit secrets)
.env
.env.local
.env.*.local
.sh
test-auth.sh
*.secrets
*.passwords
demo-passwords.*
