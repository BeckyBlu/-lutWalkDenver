
node_modules
.next

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Vendored npm package directories (installed by npm install)
@img/
@next/

# Local environment files (never commit secrets)
.env
.env.local
.env.*.local
